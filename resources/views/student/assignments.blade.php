<x-layouts.student title="{{ __('student.assignments.title') }}">
    <div class="bg-white overflow-hidden shadow-sm rounded-xl">
        <div class="p-6 text-gray-900">
            <h1 class="text-2xl font-bold mb-4">{{ __('student.assignments.title') }}</h1>
            <p>{{ __('student.assignments.welcome') }}</p>
            <p class="mt-4">{{ __('student.assignments.under_development') }}</p>
        </div>
    </div>
</x-layouts.student> 