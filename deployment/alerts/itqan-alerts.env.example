# /etc/itqan-alerts.env — Telegram dispatcher credentials.
# Same token + chat_id on every host so all alerts land in @itqan_platform_debug_bot.
# Permissions: chmod 600, owned by root:root.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
